# AGENTS.md

Durable repository guidance for Codex and other coding agents.

Keep this file compact. Put one-off task instructions, audit prompts, and large
remediation plans in task prompts or `plan.md`.

## Project Purpose

This repository packages a classroom-ready Arduino Nano 33 BLE Sense plus
phyphox kit. It maintains one canonical Arduino BLE sketch, generated core
phyphox experiments for that sketch, and a separate curated astronomy experiment
subtree for classroom analogies and bounded model experiments.

## Important Directories

- `arduino/phyphox_ble_sense/` - canonical Arduino BLE firmware.
- `src/phyphox/` - source XML for generated core sensor experiments.
- `src/phyphox/includes/` - shared XInclude XML fragments.
- `experiments/*.phyphox` - committed generated core experiments for import into
  phyphox.
- `experiments/astronomy/*.phyphox` - hand-edited astronomy classroom files;
  these are not generated by `scripts/build-phyphox.sh`.
- `experiments/phyphox_constants.json` - BLE UUID and mode metadata.
- `tools/` - XML post-processing and phyphox validation helpers.
- `scripts/` - build, validation, Arduino compile, security, and local CI
  entrypoints.
- `tests/` - pytest guardrails for XML, generated parity, physics, astronomy,
  and repository contracts.
- `docs/` - runbook, CI notes, repository map, and classroom companion docs.

## Agent Operating Boundaries

Permission boundaries: sensitive actions need explicit user approval from the
user. This includes commits, pushes, pull requests, dependency additions, Cloud
imports/reanalysis, destructive file operations, and marking scanner findings
false positive or suppressed.

## Memory Strategy

For long audits or remediation work, keep durable state in a repo-local ledger,
`plan.md`, or another named task document.

File-based notes and state tracking: update the ledger or plan as slices are
completed so the task can resume from the latest verified state. Write it down;
do not rely on mental notes or chat-only memory for resumable state.

## Commands

Install test dependencies:

```sh
python3 -m pip install -r requirements-test.txt
```

Build and generated-file checks:

```sh
make build
bash scripts/build-phyphox.sh
bash scripts/check-generated-clean.sh
```

Tests:

```sh
make test
pytest
```

Lint and format check:

```sh
make lint
ruff check .
ruff format --check .
```

Validation, compile, security, and full local CI:

```sh
make validate
make compile
make security
make ci-local
bash scripts/ci-local.sh
```

`make compile` and `scripts/compile-arduino.sh` require `arduino-cli` and compile
for `arduino:mbed_nano:nano33ble`. XML validation and builds require `xmllint`.

## Runtime Entry Points

- Arduino firmware: `arduino/phyphox_ble_sense/phyphox_ble_sense.ino`.
- Core generated phyphox experiments: `experiments/*.phyphox`.
- Core source XML: `src/phyphox/*.phyphox.xml`.
- Astronomy classroom experiments: `experiments/astronomy/*.phyphox`.
- Full local verification: `scripts/ci-local.sh`.

## Contracts

- BLE device name is `phyphox-sense`.
- BLE UUIDs must stay aligned across `experiments/phyphox_constants.json`,
  `src/phyphox/*.phyphox.xml`, generated `experiments/*.phyphox`, and
  `arduino/phyphox_ble_sense/phyphox_ble_sense.ino`, unless the task explicitly
  changes and documents the BLE contract.
- The data characteristic payload is 5 little-endian `float32` values: device
  time plus four mode-dependent channels. Phyphox packet time is app-managed.
- Core experiment mode IDs are `1` acceleration, `2` gyroscope, `3`
  magnetometer, `4` pressure, `5` temperature/humidity, `6` light/RGB, and `9`
  analog input. Modes `7` and `8` are reserved.
- `experiments/*.phyphox` are generated artifacts. Edit
  `src/phyphox/*.phyphox.xml` and rebuild unless the generator or committed
  artifact contract itself is the target.
- `experiments/astronomy/*.phyphox` are hand-edited and must keep English root
  locale plus `de` and `fr` translation blocks, unless the task explicitly
  changes and documents the astronomy locale contract.
- Astronomy files currently use phone sensors, TI SensorTag hardware, or the
  supported Owon multimeter. Do not route them through the Arduino
  `phyphox-sense` runtime unless the runtime contract is deliberately changed
  and documented.
- Arduino package and library versions in `scripts/compile-arduino.sh` are pinned
  for reproducible classroom builds. Do not upgrade them without running the
  relevant compile and experiment validation checks.

## Deprecated-Code Policy

Do not keep deprecated compatibility paths unless there is evidence they are
still required. Do not silently preserve old behavior if it is wrong; document
the compatibility impact and verification path. Do not remove pre-existing
deprecated code, archived audit files, or old compatibility material unless the
task explicitly includes that cleanup.

## Code-Change Rules

Act as a cautious senior engineer.

- Prefer the minimum code that solves the actual problem.
- No speculative features.
- No abstractions for single-use code.
- No broad rewrites without a written plan.
- Touch only files required by the task.
- Read relevant files, exports, callers, tests, shared utilities, scripts, and
  docs before editing.
- Do not assume dependencies, code paths, config, schemas, APIs, protocols, or
  storage contracts exist. Inspect them.
- Keep generated and hand-edited experiment surfaces distinct.
- Keep tests focused on why behavior matters, not only what output appears.
- Report unrelated issues instead of fixing them.

## Verification Expectations

Use the narrowest meaningful check first, then broader checks as risk increases.
For source XML or generated experiment changes, run `bash scripts/build-phyphox.sh`
and `bash scripts/check-generated-clean.sh`. For BLE, mode, or file-contract
changes, run targeted pytest coverage and `bash scripts/validate-xml.sh`. For
firmware changes, run `bash scripts/compile-arduino.sh` when `arduino-cli` is
available. Before PR-ready handoff, prefer `bash scripts/ci-local.sh`.

Do not claim completion without evidence. If any build, test, lint, migration,
edge case, hardware probe, or runtime check was skipped, say so explicitly.

## Final Response Expectations

For implementation tasks, include:

1. Files changed.
2. Why each file changed.
3. Commands run.
4. Tests/checks passed.
5. Tests/checks skipped or unavailable.
6. Remaining uncertainty.
7. Follow-up risks.

<!-- REPOWISE_AGENTS:START — Do not edit below this line. Auto-generated by Repowise. -->
## Codebase Intelligence for curious-signals (Repowise)

Indexed by [Repowise](https://repowise.dev). Last indexed: 2026-08-03 (commit 2aa3c0a). Confidence: 100%.
The MCP tools below serve pre-verified docs, symbols, history, and health from that index. Every response carries `_meta` freshness fields; a `stale_warning` appears only when a file the response actually serves changed after indexing, so silence means current.

### How to work in this repo

- **Pre-edit phase** (locate, understand, assess) is where these tools win: `get_answer` for how/where/why, `search_codebase` to find, `get_context` for a file's map, `get_risk` before touching a hotspot.
- **Edit phase**: reading a file before you edit it is correct and expected. Use these tools to decide *which* files to read and edit, not to replace that read.
- **Noisy commands** (tests, builds, `git log`/`diff`, searches, listings): prefer `repowise distill <cmd>`, the same command with its exit code preserved and errors-first compact output. A `[repowise#<ref>: N lines omitted]` marker is fully recoverable via `repowise expand <ref>` (add `-q <regex>` to filter); never re-run the command to see omitted output.

### Trust protocol

- `verified: true` means the served bytes were checked against the live tree. Never follow it with a re-read of the same lines.
- `get_answer` at `confidence: "high"` or `grounding: "extracted"` is content-grounded: cite it directly. `symbol_bodies`, `quotes`, and `code_rationale` entries are live source, so use them instead of opening the file.
- The **only** re-read triggers: `bounds: "approximate"`, `_meta.stale_warning`, `search_method: "bm25"`, `confidence: "low"`. `index_behind: true` alone is informational; the served content is unaffected by the drift.
- Not valid reasons to re-read: "just to be safe", "to see full context" (use the skeleton or a range read), "the file might have changed" (`verified` already checked).
- For exhaustive literal sweeps (rename every call site) plain text search is unbeatable, so use it. Reach for `get_context(include=["callers"])` when you need the `callers_total`/`callers_truncated` honesty signal instead of a maybe-incomplete grep.

### Tools

| Tool | When and why |
|------|--------------|
| `get_answer(question)` | First call for any how / where / why question. `confidence: "high"` or `grounding: "extracted"` is content-grounded — cite it directly. When the question names an indexed symbol, `symbol_bodies` carries its full live body (skip the `get_symbol` follow-up). Low confidence returns `best_guesses` with one-line justifications plus `code_rationale` (rationale comments mined live from candidate source). |
| `get_context(targets=[...])` | Triage card for files/modules/symbols: summary, signatures, `symbol_id`s, `hotspot` bit. File targets auto-serve a `verified` skeleton (every signature at a fraction of a full Read); `mostly_full` marks files where Read costs little more. Batch targets in one call. Opt-in blocks: `include=["callers"|"callees"|"ownership"|"decisions"|"metrics"]`. |
| `get_symbol(id)` | One verified body: `"path.py::Name"` (indexed symbol), `"path.py:140-180"` (live range read), or `"repowise#<hex>"` (omission ref). Source arrives in Read's numbered format — treat it as an already-performed Read. `truncated` responses carry a `continuation` naming the exact next range; ambiguous ids return every match in `candidates`. Index misses fall back to live-grep `fallback_lines`. |
| `search_codebase(query)` | Hybrid search, auto-routed by query shape: identifier → symbol hits (pipe `symbol_id` into `get_symbol`), path → file pages, prose → wiki-semantic. Force with `mode=symbol|path|concept|hybrid`. Concept hits carry a `sources` list; a hit whose sources are `[fts]` only is a keyword match with no semantic agreement — verify it. |
| `get_why(query, targets?)` | Why the code is shaped this way: decision records with evidence and supersession lineage, falling back to git archaeology and `code_rationale` comments. Call before refactors or pattern divergences. |
| `get_risk(targets, changed_files?)` | What history says about touching these files: churn, owners, co-change partners, blast radius. PR mode (`changed_files`) leads with a `directive` block — read `will_break` / `missing_cochanges` / `missing_tests` / `tests_to_run` first. `tests_to_run` is coverage-backed (the tests the per-test map proves exercise the changed files); empty means unknown, never no tests. To score a whole commit or diff range instead, use `get_change_risk`. |
| `get_change_risk(revspec, extensions?, exclude_patterns?)` | Pre-merge defect score for a whole commit or `base..head` range, computed from its diff shape on the live checkout (no index, no LLM). Lead with `risk_percentile` (this change ranked against sampled recent commits), summarized by `review_priority` and `classification`; `score` / `probability` / `level` are the corpus-calibrated fallback. Distinct from `get_risk`, which scores indexed files by path. A `warning` field flags an empty diff (bad revspec or over-tight extension / exclusion filters). |
| `get_health(targets?, include?)` | Health scores + findings on three dimensions (defect / maintainability / performance). Self-check the files you touched before finishing; `include=["biomarkers"|"refactoring"|"signals"]` for depth. |
| `get_dead_code()` | Confidence-tiered unreachable files / unused exports / zombie packages. For cleanup sweeps, not targeted fixes. |
| `get_overview()` | Architecture map + tool recipes. Call once, first, in an unfamiliar repo; skip it after that. |

**Compose them:** low-confidence `get_answer` then read `best_guesses[0].file`; `get_context` shows `hotspot: true` then `get_risk` before editing; `decision_records` titles then `get_why(targets=[...])`; PR review then `get_risk(targets, changed_files)` and read `directive` first. A `tombstone` error means the file moved, so follow `successor_paths`.

### Architecture
I’m using the RepoWise codebase-exploration skill to ground the overview in the indexed architecture, then I’ll verify the live entry points and build flow. RepoWise’s index is present, but its query calls were unavailable in this run. I’m falling back to the live repository’s README, build scripts, firmware, validators, and CI configuration so the page remains evidence-based. curious-signals consumes XInclude-composed phyphox XML, Arduino Nano 33 BLE Sense sensor readings, analog inputs, and hand-authored astronomy experiment XML; it expands and validates the core XML, compiles mode-selecting BLE firmware, and produces importable .phyphox experiments, live Bluetooth telemetry, astronomy classroom activities, and a deterministic browser preview.

### Key modules
- `demo` — The Experiment Demo and XML Validation subsystem is an architecturally central tooling boundary: it consumes experiment interactions…

### Entry points
- `demo/demo.js`

### Files that need care (bug-fix history first, then churn — check `get_risk` before editing)
- `tests/test_repo_guardrails.py` — 2 bug fixes, last fix 8 weeks ago; 4 commits/90d
- `tools/validate_phyphox.py` — 1 bug fix, last fix 8 weeks ago; 4 commits/90d
- `tests/test_postprocess_phyphox_xml.py` — 1 bug fix, last fix 8 weeks ago; 3 commits/90d
- `tests/test_astronomy_semantics.py` — 1 bug fix, last fix 8 weeks ago; 3 commits/90d
- `tests/test_phyphox_validate.py` — 3 commits/90d

### Code health
Three co-equal signals: defect risk 9.11/10 avg, hotspot health 5.93/10 (stable), worst `tools/validate_phyphox.py` at 3.55/10 · maintainability 9.75/10 · performance risk 2 open static I/O-in-loop / N+1 findings. Detail: `get_health()`.

### Commands
- Test: `pytest`
- Lint: `ruff check .`
- Build: `make build`

<!-- REPOWISE_AGENTS:END -->
