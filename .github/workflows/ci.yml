name: verify
on:
  push:
    branches: [dev]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: git status --short
expand-xinclude-based-ex: bootstrap
github-actions: core-build-out
string: publication
add-stricter-warning-han: publication
astronomy: maintenance
